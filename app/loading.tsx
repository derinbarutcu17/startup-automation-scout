export default function Loading() {
  return (
    <main className="page-shell loading-shell" aria-live="polite">
      <div className="loading-line" />
      <p>Loading Scout records...</p>
    </main>
  );
}
